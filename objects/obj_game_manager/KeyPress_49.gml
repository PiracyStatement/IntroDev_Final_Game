/// @description Insert description here
// You can write your code in this editor
show_debug_message(regions[region].r_name);
region++;

if regions[region].r_zone != zone
{
	zone = regions[region].r_zone;
}